# include "../includes.hpp"

std::string negation_normal_form(std::string &formula)
{
    std::string result = formula;

    // First convert Material Condition(>), Logical Equivalence(=) and Exclusive Disjunction(^) to equivalent opertations.


    return (result);
}